# 아침 브리핑 수집 프롬프트

Claude Code 클라우드 스케줄 작업(`morning-briefing` routine, 매일 오전 7시 KST)이 실행하는 원문.
수정할 때는 **이 파일만 고치면 됨** — routine은 매번 이 파일을 새로 읽어서 실행하므로 별도로
routine 설정을 바꿀 필요 없음.

---

오늘 아침 브리핑을 만들어줘. 오늘 날짜/시각은 셸에서 직접 확인해(`date -u` 등, UTC 기준으로
72시간 계산). 다음 3개 카테고리로 최신 뉴스를 웹서치해서 수집해:

1. 채용·HR·조직문화
2. 국내 대기업 사업동향 (삼성, SK, 현대차, **LG전자** 등 포함해서 검색할 것)
3. AI·테크 전반

## 기사 선정 규칙

- **카테고리당 헤드라인 최대 2개.** 조건에 맞는 기사가 1개뿐이면 1개만, 하나도 없으면
  그 카테고리 자체를 아래 JSON의 `categories` 배열에서 **통째로 생략**해
  (빈 `items: []`로 남기지 말고, 카테고리 항목 자체를 넣지 않을 것).
- **최신성: 실행 시점 기준 최근 72시간(3일) 이내에 발행된 기사만.** 검색 결과에서
  발행일을 확인할 수 없는 기사이거나 72시간을 넘긴 기사는 후보에서 제외해. 발행일이
  애매하면 포함하지 말고 제외하는 쪽으로 판단해.
- **중복/후속 보도 처리:**
  - 오늘 모은 후보들 중 언론사·링크가 다르더라도 같은 사건·이슈를 다루는 기사면 하나로
    묶어서, 그 중 가장 최신/구체적인 기사 하나만 후보로 남겨.
  - **작업을 시작할 때 먼저 `git show HEAD:latest-briefing.json`으로 어제자 브리핑을
    확인해.** 오늘 후보 기사가 어제 이미 다룬 것과 같은 사건이면 새 헤드라인으로 넣지 마.
    단, 수치 변경·잠정합의→최종타결 같은 결과 발표 등 유의미한 진전이 있으면 예외적으로
    포함 가능 — 이때는 headline 맨 앞에 `[업데이트]`를 붙이고 summary를 1문장으로 짧게 써.

각 항목은:
- headline (제목, 업데이트인 경우 `[업데이트] ` 접두어)
- summary (2문장 이내 요약, 업데이트는 1문장)
- source (출처 매체명)
- url (기사 링크)
- hrComment (선택, HR 관점 코멘트 — 있으면만 추가)

전체 분량은 5분 내 읽을 수 있는 정도로 제한해줘.

다 모았으면 아래 스키마의 JSON으로 구조화해 (조건에 맞는 기사가 없는 카테고리는 생략):

```json
{
  "date": "YYYY-MM-DD",
  "categories": [
    {"name": "채용·HR·조직문화", "items": [...]},
    {"name": "국내 대기업 사업동향", "items": [...]},
    {"name": "AI·테크 전반", "items": [...]}
  ]
}
```

**저장 방법 (중요, 아카이브 구조):**

브리핑은 매일 새 파일로 저장돼. 오늘 날짜를 `YYYY-MM-DD`라고 할 때:

1. 위 JSON을 `archive/YYYY-MM-DD.json`으로 저장해 (파일이 이미 있으면 덮어써도 됨 — 같은 날
   재실행된 경우).
2. `archive/index.json`을 읽어서(없으면 빈 배열로 시작) 오늘 날짜를 배열에 추가해. **이미
   들어있으면 중복으로 추가하지 마.** 배열은 날짜 오름차순으로 정렬해서 저장해.
   예: `["2026-08-25", "2026-08-26", "2026-08-27"]`
3. 아래처럼 커밋하고 push까지 반드시 끝내:

```
git add archive/YYYY-MM-DD.json archive/index.json
git commit -m "브리핑 업데이트: YYYY-MM-DD"
git push origin main
```

(참고: 저장소 루트에 있던 `latest-briefing.json`은 더 이상 안 씀. 웹앱은 `archive/index.json`과
`archive/YYYY-MM-DD.json`만 읽음.)

push까지 성공해야 웹앱(GitHub Pages)에 반영돼. push가 실패하면(권한 오류 등) 그 에러 메시지를
그대로 결과에 남겨줘 — 사용자가 원인을 파악할 수 있어야 해.
